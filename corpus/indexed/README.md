# Indexed corpus (placeholder)

A search index over `../raw/` and `../community/` goes here once the corpus is large enough to warrant one (mirrors `FortiAIGate-SDK/corpus/indexed/`).

For now the corpus is small enough to read directly — `/sase-lookup <topic>` searches `raw/fortinet-docs/` and `api/reference/` straight. Generate an index when the doc count grows or when we add raw HTML→MD page captures.
